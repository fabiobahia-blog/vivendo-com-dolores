// Reações sempre na nuvem — usa as mesmas credenciais do mural (mural.config.js).
// Alternativa: "upstash" + REACTIONS_UPSTASH_* (veja docs/REACTIONS-UPSTASH.md).
window.REACTIONS_BACKEND = "supabase";

window.REACTIONS_UPSTASH_URL = "";
window.REACTIONS_UPSTASH_TOKEN = "";
